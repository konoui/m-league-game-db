---
name: review-query
description: queries/ の既存 SQL クエリ例をレビューし、命名・plan・SQL の意味的な問題を修正する。クエリの見直し、品質チェック、DB スキーマ変更後の追従を頼まれたときに使う。
---

# クエリレビュー

`scripts/validate.py` が検査できない、意味レベルの問題を見つけて直す。

## 参照するもの

- `../create-query/references/query-format.md`: 形式・命名・品質基準
- `../create-query/references/analysis-perspectives.md`: 指標の定義
- `TABLE.md`: テーブル定義

## 手順

### 1. 対象を決めて機械検証する

対象が指定されていなければ、全件を対象にする。

```bash
uv run scripts/validate.py [queries/<description> ...]
```

FAIL があれば先に直す。`tables` の不一致は `--fix` で直る。

### 2. クエリごとに確認する

複数を対象にする場合も、1 件ずつ順に確認する。

**description**

- 内容を正しく表しているか（例: 実際は率を出しているのに「回数」になっていないか）
- 取得系（`を取得する`）で、記録・統計内容が具体的すぎないか。具体的すぎる場合は `基本情報` / `試合情報` / `統計情報` / `詳細情報` にまとめる

**plan**

- 目的・指標定義・設計方針・出力方針がそろっているか
- SQL の実際の処理と食い違っていないか（並び順、除外条件、件数制限など）
- SQL の構文や識別子を使わず、自然言語で書けているか

**query.sql**

- 指標の定義が `analysis-perspectives.md` や一般的な麻雀の定義と合っているか
- JOIN による行の重複で集計が水増しされていないか
- 出力カラムの別名が値の意味と合っているか（例: 4着率に「ラス回避率」と付けていないか）
- 計算しているのに出力していない、不要な処理がないか
- CTE・JOIN 群・カラム群に、意図を説明する `-- ` コメントがあるか

**checks**

- `unique` が description の「〜別」の粒度と一致しているか
- 率・平均順位などの値域、部分 ≤ 全体、内訳の合計 = 全体といった、書ける性質が抜けていないか
- 現在のデータに合わせただけで、ドメイン上の根拠がないルールになっていないか（例: 実測の最大値をそのまま上限にしている）

疑わしい点は `sqlite3 -readonly -header -column database.sqlite3` で実際に値を確かめてから判断する。

### 3. 修正する

- 明らかな誤り（表記ゆれ、plan と SQL の食い違い、コメント不足）はそのまま直す
- 集計結果が変わる修正は、修正前後の結果の差（件数、代表的な値）を示してユーザーに確認してから適用する
- `description` を変えるときはディレクトリ名も変える（`git mv queries/<旧> queries/<新>`）

修正後に次を実行する。

```bash
uv run scripts/validate.py --fix queries/<description>
uv run scripts/render.py
```

### 4. 報告する

クエリごとに、直した点、ユーザーの判断を待つ点、問題なしのいずれかを一覧で報告する。
