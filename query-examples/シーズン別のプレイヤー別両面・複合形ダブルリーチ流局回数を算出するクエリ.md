## 説明
シーズン別のプレイヤー別両面・複合形ダブルリーチ流局回数を算出するクエリ

## SQL クエリ
```sql
-- リーチイベントに対応するリーチ宣言打牌イベントを特定する
WITH reach_discard AS (
    SELECT
        re.actor_player_id,
        e_r.kyoku_id,
        e_d.id AS discard_event_id
    FROM reach_event re
    JOIN event e_r ON re.event_id = e_r.id
    -- リーチイベントの直後のイベントがリーチ宣言打牌
    JOIN event e_d ON e_d.kyoku_id = e_r.kyoku_id
        AND e_d.event_order = e_r.event_order + 1
    WHERE re.is_accepted = 1
),
-- ダブルリーチかつ両面・複合形待ちの局・プレイヤーを特定する
double_reach_tenpai AS (
    SELECT DISTINCT
        rd.actor_player_id,
        rd.kyoku_id
    FROM reach_discard rd
    -- 打牌回数によるダブルリーチ判定
    JOIN player_state ps
        ON ps.event_id = rd.discard_event_id AND ps.player_id = rd.actor_player_id
    -- 待ちタイプの確認
    JOIN player_tenpai_state pts
        ON pts.event_id = rd.discard_event_id AND pts.player_id = rd.actor_player_id
    WHERE ps.turn_number = 1
      AND pts.waiting_type IN ('両面', '複合形')
),
-- 流局した局を特定する
ryukyoku_kyoku AS (
    SELECT DISTINCT e.kyoku_id
    FROM ryukyoku_event re
    JOIN event e ON re.event_id = e.id
),
-- ダブルリーチかつ両面・複合形待ちで流局した局・プレイヤーを特定する
double_reach_ryukyoku AS (
    SELECT drt.actor_player_id, drt.kyoku_id
    FROM double_reach_tenpai drt
    -- 流局した局との突き合わせ
    JOIN ryukyoku_kyoku rk ON rk.kyoku_id = drt.kyoku_id
)
-- シーズン別・プレイヤー別に流局回数を集計する
SELECT
    ls.start_year || '-' || ls.end_year AS シーズン,
    p.name AS プレイヤー名,
    t.name AS チーム名,
    COUNT(*) AS 流局回数
FROM double_reach_ryukyoku drr
-- 局・試合・シーズン情報の結合
JOIN kyoku k ON drr.kyoku_id = k.id
JOIN game g ON k.game_id = g.id
JOIN season_stage ss ON g.season_stage_id = ss.id
JOIN league_season ls ON ss.league_season_id = ls.id
-- プレイヤー情報の結合
JOIN player p ON drr.actor_player_id = p.id
-- チーム所属情報の結合
JOIN player_team pt ON drr.actor_player_id = pt.player_id
    AND ls.start_year >= pt.joined_season_year
    AND ls.start_year <= pt.left_season_year
JOIN team t ON pt.team_id = t.id
GROUP BY ls.id, drr.actor_player_id
ORDER BY ls.start_year DESC, 流局回数 DESC;
```

## 参照テーブル
- reach_event
- event
- player_state
- player_tenpai_state
- ryukyoku_event
- kyoku
- game
- season_stage
- league_season
- player
- player_team
- team
